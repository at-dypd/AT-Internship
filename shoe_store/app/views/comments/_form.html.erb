<%= form_for([@product, @product.comments.build]) do |f| %>
  <p>
    <%= f.text_area :content, :class => 'nifty-form'%>
  </p>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :product_id, value: params[:id] %>
  <p><%= f.submit %></p>
<% end %>

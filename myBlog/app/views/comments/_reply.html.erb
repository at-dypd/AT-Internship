<% binding.pry %>
<%= form_for([current_user, @post, @post.comments.build] ) do |f| %>
  <p>
    <%= f.hidden_field :comment_id, :value => @comment.id %>
    <%= f.label :content %>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
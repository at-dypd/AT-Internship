<% binding.pry %>
<%= form_for([current_user, @post, @comment] , action: :create, html: {multipart: true}) do |f| %>
  <p>
    <%= f.hidden_field :id, :value => @comment.id %>
    <%= f.label :content %>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>